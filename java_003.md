## 1. 추상화 (Abstraction)
"프로그램에 필요한 공통점을 추출하고, 불필요한 특성은 제거하는 과정"

대상의 불필요한 세부 구현은 숨기고, 공통적인 인터페이스(선언부)만 정의하여 설계의 틀을 잡는 것

예시)
학생 관리 프로그램'을 만든다면 필요한 정보는 이름, 성별, 주민 번호, 학번 등이 될 수 있다. 학생의 시력이나 좋아하는 음식 같은 정보는 불필요하므로 제거하게 됨.


## 2. 캡슐화 (Encapsulation)
"데이터(필드)와 기능(메서드)을 하나의 단위로 묶고, 내부 상태를 외부에 직접 노출하지 않는 것"

핵심 개념: 정보 은닉(Data Hiding)을 통해 외부에서 객체 내부 데이터를 직접 수정하지 못하게 막고, 검증된 메서드를 통해서만 접근하도록 제한합니다.


예시) 
접근 제어자(private, protected, public), Getter/Setter 메서드


## 3. 상속 (Inheritance)
"프로그래밍에서는 기존의 클래스(부모)가 가지고 있는 속성(필드)과 기능(메소드)을 새로운 클래스(자식)가 그대로 물려받는 것"


### 장점
1) 적은 양의 코드로 새로운 클래스 작성 가능
	=> 부모의 코드를 그대로 가져다 쓰므로 중복이 제거
2) 코드를 공통적으로 관리
	=> 부모 클래스 한 곳만 수정하면 이를 상속받은 모든 자식 클래스에 자동 반영되므로, 코드의 재사용성을 높이고 유지보수가 용이해집니다.

### 용어
	* 부모 클래스 = 상위(Super) 클래스
	* 자식 클래스 = 하위(Sub) 클래스

### 표현식 
    => 자식 클래스 선언 시 `extends` 키워드를 사용합니다.
	[접근제한자] class 자식클래스 extends 부모클래스 { ... }

### 5) 메소드 오버라이딩 : 부모 클래스로부터 물려받은 메소드의 내용이 자식 클래스에 맞지 않을 때, 자식 클래스 내부에서 메소드를 재정의하여 덮어쓰는 기술
	-> 성립 조건
		[1] 부모 메소드와 이름이 동일해야 한다.
		[2] 부모 메소드와 매개변수의 개수, 타입, 순서가 동일해야 한다.
		[3] 부모 메소드와 반환형이 동일해야 한다.
		[4] 자식 메소드의 접근 제한자는 부모의 접근 제한자보다 범위가 같거나 넓어야 한다.
		[5] 부모 메소드보다 더 많은 예외(Exception)를 선언할 수 없다.

### 오버로딩(Overloading) vs 오버라이딩(Overriding)
	* 오버로딩 : 한 클래스 내에서 이름이 같고 매개변수가 다른 메소드를 여러 개 만드는 것
	* 오버라이딩 : 부모에게 받은 메소드를 자식이 완전히 똑같은 형태로 재정의하는 것

## 4. 다형성 (Polymorphism)
"하나의 객체(참조변수)가 여러 가지 형태를 가질 수 있는 능력을 뜻합니다. 자바에서는 부모 타입의 참조변수로 자식 타입의 인스턴스를 참조할 수 있도록 하여 다형성을 구현"

예시) 
// [일반적인 객체 생성]
Sonata s = new Sonata();

// [다형성이 적용된 객체 생성] 부모인 Car 타입으로 자식 객체들을 참조 가능
Car c1 = new Sonata();
Car c2 = new Avante();
Car c3 = new Grandure();

### 활용 예시)
1) 다형성과 객체 배열
	=>  다형성을 활용하면 부모 타입의 배열 하나로 모든 자식 객체들을 묶어서 관리할 수 있음
2) 매개변수와 다형성
	=> 메소드를 정의할 때 매개변수 타입을 부모 타입으로 지정하면, 단 하나의 메소드로 모든 자식 객체들을 매개변수로 받아 처리할 수 있음

Java 구현 방식: 오버라이딩(Overriding), 오버로딩(Overloading), 부모 타입 변수에 자식 객체 할당 (업캐스팅)

// 부모 타입(Engine) 변수에 자식 객체(ElectricEngine) 할당
Engine myEngine = new ElectricEngine();

// 실제 생성된 ElectricEngine의 start()가 실행됨 (다형성)
myEngine.start();
