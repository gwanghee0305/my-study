## 1. 호이스팅 (Hoisting)

⇒ 호이스팅은 코드가 실행되기 전, 변수 및 함수 선언을 메모리에 미리 등록하여 코드의 최상단으로 끌어올려진 것처럼 동작하는 현상을 의미합니다.

### 1) var

- var 키워드선언과 초기화(undefined)가 동시에 이루어집니다.
- 선언 전에 참조하면 에러가 나지 않고 undefined를 반환합니다.

### 2) let / const

let, const 키워드 선언 단계와 초기화 단계가 분리되어 실행됩니다.
호이스팅 자체는 일어나지만, 초기화 전까지 TDZ(Temporal Dead Zone, 일시적 사체 지대)에 빠져 참조 시 ReferenceError가 발생합니다

### 3)  함수의 호이스팅

선언적 함수 : 함수 전체가 호이스팅되어 선언 전에도 호출 가능합니다.

함수 표현식: 변수 선언 호이스팅 규칙을 따르므로 선언 전에 호출하면 TypeError 또는 ReferenceError가 발생합니다.

=================================================================

## 2. 클로저 (Closure)

⇒ 내부 함수가 외부 함수의 변수를 기억하고 계속 사용할 수 있는 기능입니다. 외부 함수의 실행이 끝난 뒤에도 필요한 데이터를 유지할 수 있습니다. 즉, 함수가 자신이 만들어질 당시의 변수를 기억하는 것입니다.

외부에서 특정 데이터에 직접 접근하는 것을 막고(private 변수 효과), 전역 변수의 오염 없이 상태 변화를 유지할 때 활용합니다.

### 1) 클로저를 사용하는 이유

#### 상태 유지

현재 상태를 기억하고 변경된 최신 상태를 유지할 때 사용합니다.

#### 전역 변수 사용 억제

 전역 변수를 남발하면 의도치 않은 오염이 생길 수 있으므로, 특정 함수만 접근할 수 있는 안전한 상태로 관리합니다.

#### 정보 은닉 (캡슐화)

 자바스크립트에서 private 변수처럼 외부 직접 접근을 막고 지정된 메서드를 통해서만 제어하도록 만듭니다.